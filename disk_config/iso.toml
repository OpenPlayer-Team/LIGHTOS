# LIGHTOS ISO disk configuration
# Per build locale: docker build -t lightos:latest .
# Per build ISO: just build-iso

[customizations]
hostname = "lightos"

# Keep installer interactive - DO NOT add kickstart for fully manual installation