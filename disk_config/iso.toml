# LIGHTOS ISO disk configuration
# Per build locale: docker build -t lightos:latest .
# Per build ISO: just build-iso

[customizations]
hostname = "lightos"

# Memtest86+ will be added to GRUB after install via /etc/grub.d/05memtest86+.cfg